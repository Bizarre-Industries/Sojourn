import Foundation

/// Homebrew-specific operations invoked directly (not via mpm).
/// Used by BootstrapService for the .pkg installer flow.
/// See docs/ARCHITECTURE.md section 9.
actor BrewService {
  init() {}
}
