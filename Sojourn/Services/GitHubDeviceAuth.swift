import Foundation

/// OAuth Device Flow for github.com. URLSession + Keychain. client_id only.
/// See docs/ARCHITECTURE.md section 5.4 (Authentication) and section 14 risk 7.
actor GitHubDeviceAuth {
  init() {}
}
