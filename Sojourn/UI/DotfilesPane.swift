import SwiftUI

/// Managed-file tree from chezmoi managed. See section 5.2.
struct DotfilesPane: View {
  @Environment(AppStore.self) private var store

  var body: some View {
    EmptyView()
  }
}
