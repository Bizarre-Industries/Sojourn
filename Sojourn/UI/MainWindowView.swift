import SwiftUI

/// NavigationSplitView root. 3-pane layout per section 11.
struct MainWindowView: View {
  @Environment(AppStore.self) private var store

  var body: some View {
    EmptyView()
  }
}
