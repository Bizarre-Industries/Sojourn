import SwiftUI

/// Modal on gitleaks findings. See section 5.3 and section 10.
struct SecretPromptSheet: View {
  @Environment(AppStore.self) private var store

  var body: some View {
    EmptyView()
  }
}
